<!-- devstandard-pointer (managed by reference/harness-codex.md) -->
# For a Codex (or other non-Claude) session working on DevStandard itself

Before your first response in this repo, read this file IN FULL — it is the development method you
operate under, and you must follow it:

    core.md

Then read `reference/harness-codex.md` for the Codex name mappings. Do not answer until you have read
`core.md`.

(This is DevStandard's own repo, so the pointer names the live `core.md` here rather than an installed
copy — this file is both the dogfood and the worked example the Codex setup step in
`reference/harness-codex.md` generates for a consuming repo. On Claude Code, delivery is the
`SessionStart` hook instead and this file is not the mechanism.)
